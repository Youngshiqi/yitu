# Agent 模块介绍

## 1. 这个模块是做什么的

简单说，这是一个**物流寄件 AI 助手**。用户在一个对话窗口里可以做三件事：

一是**问问题**——比如"北京寄上海多少钱？""化妆品能不能寄？""我的包裹到哪了？"，AI 直接回答。

二是**寄件**——用户说"我要寄个包裹"，AI 一步步引导他补充收件地址、重量、尺寸，然后自动算运费，让用户确认，确认完就创建运单。整个过程像有一个耐心的客服在帮他填单子。

三是**被拦截**——如果用户试图让 AI 绕过规则，或者查别人的运单，系统会直接拒绝，不会把请求发给大模型。

---

## 2. 整体架构

这个模块按分层组织的，核心思想是**每层只关心自己的事**：

```
用户请求
  │
  ▼
api/          ← HTTP 和 SSE 流式传输，只做参数校验、身份认证
  │
  ▼
runtime/      ← 连接 API 和图执行引擎的胶水层，组装依赖、驱动图运行
  │
  ▼
workflow/     ← 图本身：10 个节点、7 条条件边，定义整个 AI 工作流
  │
  ├→ capabilities/  ← 节点调用的业务能力（消息存取、知识搜索、寄件编排）
  ├→ tools/         ← 大模型可调用的工具（查运单、查地址簿、查运费规则等）
  ├→ domain/        ← 数据模型和领域服务（草稿、授权、记忆）
  └→ infrastructure/← 基础设施（模型适配、checkpoint 存储、RAG、脱敏）
```

---

## 3. 核心：一张图搞定所有流程

整个 AI 工作流就是一张状态图，有 10 个节点。每次用户发一条消息，图从 START 跑到 END。

```
START
  │
  ▼
① 加载上下文        ← 从数据库拉历史消息，清空上轮标记
  │
  ▼
② 安全守卫          ← 正则匹配注入攻击和跨用户访问，命中直接拒绝
  │
  └─ 通过
  ▼
③ 大模型思考        ← 调用 LLM，带上系统提示词 + 历史消息 + 6 个工具
  │
  ├── 纯文本回复 ──→ ⑨ 持久化回复 → END
  │
  ├── 调了工具 ──→ ④ 执行工具 → ③ 回到大模型（ReAct 循环）
  │
  └── 触发寄件 ──→ ⑤ 处理草稿字段
                      │
                      ├── 缺字段 ──→ ⑨ 提示用户补充
                      └── 齐全 ──→ ⑥ 生成报价
                                      │
                                      └─→ ⑦ 人工确认 ⭐暂停
                                            │
                                            ├── 取消 ──→ ⑨
                                            └── 确认 ──→ ⑧ 创建运单
                                                            │
                                                            └─→ ⑨ → END
```

**关键设计**：图的 State 是 `AssistantState`，只放可序列化的数据（消息文本、工具调用列表、寄件进度、错误信息等）。数据库连接、模型客户端、用户身份这些不能序列化的东西，通过 LangGraph 的 Runtime 机制注入到每个节点，不进入 checkpoint。

---

## 4. 一条消息的完整旅程

从一个 HTTP 请求到响应的全过程：

**第一步**：请求到达 FastAPI 端点。FastAPI 的依赖注入自动做了这几件事——校验用户 JWT 身份、获取数据库会话、获取模型适配器、获取图执行器（进程级单例，所有请求复用）。

**第二步**：组装运行时上下文。把用户身份、数据库会话、模型客户端、各项业务服务打包成一个 `AgentRuntimeContext` 对象，这个对象会在图执行时注入到每个节点。

**第三步**：驱动图执行。`AgentGraphRunner` 是图的驾驶员。它首先检查上次是否暂停在确认节点等人——如果是，就判断用户这次发的是"确认"还是"取消"，用对应的 decision 恢复图；如果不是，就作为新回合跑完整张图。

**第四步**：图跑起来。LangGraph 按拓扑自动执行节点。第一个节点加载历史消息，第二个节点做安检，第三个节点调用大模型。大模型返回后，路由函数根据返回内容决定下一步——是直接回复，还是执行工具，还是进入寄件流程。

**第五步**：流式输出。大模型不是一次性吐完的，而是逐字逐 token 生成。每个 token 通过 `runtime.stream_writer()` 实时推给前端，用户看到的是打字效果。最终回复由 `finalize_turn_node` 持久化到数据库。

---

## 5. 大模型怎么被调用

大模型调用在 `assistant_agent_node` 这个节点里。它做了几件事：

**组装消息列表**：`[系统提示词, ...历史消息]`。系统提示词定义了 AI 的身份——Yitu 物流助手，只做物流相关的事，表达要亲和自然，结论要准确克制。

**给大模型一份工具清单**：6 个工具，包括搜索知识库、查运单、查地址簿、查运费规则、查身份，还有一个特殊的 `start_shipment`（这不是真正可执行的工具，而是告诉系统"用户想寄件了，我提取了这些字段"）。

**大模型自己决定**：是直接回复文本，还是调用某个工具。比如用户说"帮我查一下我的运单"，大模型就会返回一个 `get_own_shipment` 的工具调用，而不是瞎编。

**流式输出**：大模型一边生成一边推 token，用户实时看到打字效果。

**解析结果**：大模型返回后，节点判断——纯文本就直接作为回复；有工具调用就交给 `assistant_tools_node` 执行后再回来；有 `start_shipment` 就进入寄件流程。

---

## 6. RAG 检索：三步走

用户问"化妆品能不能寄"时，AI 不能瞎编，必须从已发布的物流规则文档中找到确切答案。这就是 RAG 做的事。

### 第一步：查询改写

用户口语化的表达（"什么东西不能寄"）先被 LLM 改写为检索友好的关键词（"禁止寄递物品"）。改写超时 4 秒，失败了就退回原始查询，不会阻塞检索。

### 第二步：混合检索

这里用了两路召回，然后加权融合。

**向量检索**：用阿里云百炼的 qwen3.7-text-embedding 模型把查询文本变成 1024 维向量，在 PostgreSQL 中用 pgvector 插件做余弦相似度检索。向量检索的好处是语义理解——"寄不了"和"禁止寄递"在向量空间里很近。

**关键词检索**：用 jieba 分词，过滤掉"哪些""什么""吗"这类停用词，在 PostgreSQL 中做全文检索。对"有哪些禁止寄递的物品"这种列举型问题，会自动追加"目录""指导目录"锚点词，让目录章节的 chunk 排在前面。

**融合**：向量分 0.55 权重 + 关键词分 0.45 权重，两路取并集，按融合分降序。

### 第三步：精排

融合后的 top 30 候选再喂给 LLM 打分精排。LLM 给每个候选输出 0 到 1 的相关性分数，超时 6 秒，失败回退。最终取 top 5 返回。

**索引是怎么建的**：文档上传后按 Markdown 结构切块，每块 800 字符、100 字符重叠。对法规类文档做了特殊处理——条号行、列举项合并到同一块，避免"第三条"和后面的正文被切成两个碎片。每块都生成向量嵌入和分词搜索 tokens。

---

## 7. 记忆系统：短期 vs 长期

### 短期记忆：对话上下文

就是最近 20 条消息的历史。每次调用大模型时，历史消息作为上下文一起发过去。工具执行结果也在这 20 条里——大模型能看到"我刚才帮你查了运费规则，结果是……"，然后基于这个继续推理。

工具结果做了截断（8000 字符），所有内容经过脱敏处理（隐藏密钥、JWT、手机号、邮箱）。

### 长期记忆：用户偏好

用户说过的偏好可以存为持久记忆，跨会话生效。比如用户说过"我一般从北京海淀区发货"，下次再寄件时 AI 就能记住。

技术实现上用 pgvector 存向量，召回时按 cosine 距离排序——语义相关的排在前面。向量生成失败就降级为按时间排序。记忆内容在存储前会检查是否含敏感信息，含则拒绝。

---

## 8. 人工确认（HITL）

寄件报价生成后，AI 不会直接下单，而是暂停等用户确认。这是图里唯一的人工暂停点。

实现用的是 LangGraph 的 `interrupt()` 方法。调用后图执行暂停，当前状态保存到 checkpoint，前端收到一张确认卡片（含寄件摘要和金额）。

用户回复"确认"或"取消"后，图的执行器检测到 pending interrupt，用 `Command(resume={"decision": "confirm"})` 恢复图。确认就走建单流程，取消就结束。如果用户发了无关内容（比如"等等，先帮我查个东西"），系统会先结束旧的确认等待，再开新的话题。

确认词是硬编码的白名单：确认、确认寄件、确认下单、同意、confirm；取消、取消寄件、不要了、cancel。

---

## 9. 安全设计

安全是分层做的：

**第一层，正则安检**：在请求发给大模型之前，用正则匹配明显注入攻击（"忽略系统指令""泄露提示词"）和跨用户访问（"查其他客户的运单"），命中直接拒绝。

**第二层，工具约束**：大模型调用的工具参数要经过校验，禁止传入 `actor_id`、`user_id` 等信任参数。工具执行时强制校验所有权——只能查自己的运单、自己的地址簿。

**第三层，隐私脱敏**：所有发给大模型和从大模型收到的内容，都经过确定性正则脱敏，隐藏密钥、JWT、手机号、邮箱。

**第四层，授权一次性消费**：建单不是大模型直接做的，而是通过签发授权 → 消费授权的机制。授权 5 分钟过期、版本绑定、行锁消费，防止重复建单或篡改。

**第五层，预算限制**：大模型最多思考 8 轮、最多调用 4 次工具，防止无限循环消耗资源。

---

## 10. 技术栈

| 组件 | 技术选型 |
|------|----------|
| 工作流引擎 | LangGraph（StateGraph + checkpoint） |
| 大模型 | OpenAI 兼容 API（支持 DeepSeek） |
| 向量嵌入 | 阿里云百炼 qwen3.7-text-embedding（1024 维） |
| 向量存储 | PostgreSQL + pgvector |
| 全文检索 | PostgreSQL tsvector + jieba 分词 |
| 数据库 | PostgreSQL（SQLAlchemy 异步） |
| 通信 | FastAPI + SSE 流式 |
| 部署 | 进程级单例图 + 共享 PostgreSQL checkpoint |

---

## 11. 一句话总结

**用户发一条消息，经过安检后交给大模型，大模型决定直接回答还是查资料还是帮用户寄件——所有流程由一张状态图控制，知识从 RAG 检索获取，用户偏好从长期记忆召回，关键操作要人工确认，安全校验贯穿始终。**