# Yitu 项目开发规范

本文档是 Yitu 项目的强制开发约束。参与项目的开发者和 AI 编码 Agent 在设计、实现、测试、审查与交付时均须遵守。



## 代码与中文注释

- 源码标识符、函数名、类名、数据库字段和 API 错误码使用英文。
- 新增代码注释和 docstring 使用简体中文。
- 注释重点解释设计原因、领域约束、权限边界、状态转换、事务、并发、幂等以及 Agent 行为。
- 不为显而易见的语句添加逐行翻译式注释，避免注释噪声。
- 修改行为时同步更新相关注释；禁止保留与实际行为不一致的陈旧注释。
- 公共模块、公共服务和复杂领域操作应提供中文 docstring，说明职责、参数约束、返回结果和主要异常。


## 数据、时间与安全

- 金额以最小货币单位的整数存储，不使用浮点数。
- 业务时间统一使用 `Asia/Shanghai`，禁止无时区时间进入业务模型。
- PostgreSQL 时间字段使用 `timestamptz`，API 时间输出必须带 `+08:00` 偏移。
- 物流轨迹、审计记录和支付流水采用追加式记录，不覆盖历史事实。
- 电话、地址和其他个人信息在日志、普通轨迹及模型上下文中按规则脱敏。
- 所有外部输入必须经过 Pydantic 和领域服务双层约束，不信任前端校验结果。



## Git 与变更控制

- 保持提交小而聚焦，一个提交只表达一个可验证的意图。
- 不覆盖或删除用户已有变更，不使用破坏性 Git 命令。
- 提交前检查差异、格式、类型和测试结果。
- 数据库模型变化必须同时提交对应 Alembic 迁移和测试。
- 实现范围以已确认的设计、PRD 和当前阶段计划为准；扩大范围前先取得用户确认。


