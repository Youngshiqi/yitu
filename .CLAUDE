# Yitu 项目开发规范

本文档是 Yitu 项目的强制开发约束。参与项目的开发者和 AI 编码 Agent 在设计、实现、测试、审查与交付时均须遵守。

## 1. 开发策略

- 采用“阶段化后端优先”：先完成并验证完整后端，再统一整理和冻结 API 文档，最后开发前端。
- 后端按工程基础、物流核心、商业与履约可靠性、知识库与 RAG、AI Agent、后端收口六个阶段推进。
- 每个阶段必须具备可执行的数据库迁移、自动化测试和 HTTP 演示路径。
- 不采用一次性生成整个项目的方式。每个阶段验收通过后才能进入下一阶段。
- 优先保持模块边界清晰，第一版采用模块化单体和独立异步 Worker，不提前拆分微服务。

## 2. 用户操作与阻塞说明

- 不得假设用户已经完成任何本机 GUI、第三方网站或账户操作。
- 需要用户启动 Docker Desktop、登录服务、创建资源、授权、付费、提供配置或作出业务选择时，必须明确暂停并说明。
- 操作说明必须包含：操作原因、打开位置、具体步骤、成功判据，以及用户完成后需要回复的信息。
- 用户说明自己不会主动执行未被明确告知的操作，因此所有必要的人工步骤都必须主动指出。
- 能由 Agent 在工作区安全完成的操作应由 Agent 完成，不把常规终端工作转交给用户。

## 3. 密钥与敏感配置

- 不要求用户在聊天中粘贴 API Key、密码、访问令牌或其他秘密。
- 所有秘密通过本地 `.env` 或等效的秘密管理机制提供，应用仅通过环境变量读取。
- `.env` 必须被 Git 忽略；仓库只提交不含真实秘密的 `.env.example`。
- 日志、异常、测试快照、数据库种子、API 响应和提交记录不得包含真实秘密。
- 需要密钥时，必须说明服务商、用途、环境变量名称、配置位置和验证方法。
- 云模型请求遵守数据最小化原则，认证秘密不得发送给模型。

## 4. AI Agent 核心要求

- AI Agent 是本项目的重点学习模块，实现时优先保证结构清晰、可解释和可测试。
- 模型适配、LangGraph 编排、知识检索、工具适配和确定性业务服务必须保持明确代码边界，具体业务行为以 PRD 和设计文档为准。
- LangGraph 的状态、节点、条件边和中断恢复应使用清晰命名，并为非直观逻辑提供中文 docstring。
- 模型、检索和工具接口必须可替换、可注入固定测试实现，自动化测试不得强依赖真实云模型。
- Agent 的关键执行步骤应具备结构化追踪，便于定位模型、检索、工具和业务服务之间的问题。
- 每个 Agent 子模块完成后，应提供简明中文说明和可重复的演示案例，帮助用户理解设计与执行过程。

## 5. 代码与中文注释

- 源码标识符、函数名、类名、数据库字段和 API 错误码使用英文。
- 新增代码注释和 docstring 使用简体中文。
- 注释重点解释设计原因、领域约束、权限边界、状态转换、事务、并发、幂等以及 Agent 行为。
- 不为显而易见的语句添加逐行翻译式注释，避免注释噪声。
- 修改行为时同步更新相关注释；禁止保留与实际行为不一致的陈旧注释。
- 公共模块、公共服务和复杂领域操作应提供中文 docstring，说明职责、参数约束、返回结果和主要异常。

## 6. API 契约与错误处理

- 开发过程中同步维护 FastAPI 请求模型、响应模型、状态码和 OpenAPI 元数据。
- 正式 API 文档在完整后端通过验收后集中整理、审核并冻结。
- 前端只依赖冻结后的 OpenAPI 契约，不自行推导权限或状态转换。
- 错误响应使用统一结构和稳定的英文错误码，同时提供适合用户理解的中文消息。
- 每个改变状态的接口必须校验身份、权限范围、资源归属、当前状态和幂等键。

## 7. 数据、时间与安全

- 金额以最小货币单位的整数存储，不使用浮点数。
- 业务时间统一使用 `Asia/Shanghai`，禁止无时区时间进入业务模型。
- PostgreSQL 时间字段使用 `timestamptz`，API 时间输出必须带 `+08:00` 偏移。
- 物流轨迹、审计记录和支付流水采用追加式记录，不覆盖历史事实。
- 电话、地址和其他个人信息在日志、普通轨迹及模型上下文中按规则脱敏。
- 所有外部输入必须经过 Pydantic 和领域服务双层约束，不信任前端校验结果。

## 8. 测试与阶段验收

- 功能和缺陷修复优先采用测试驱动方式：先写失败测试，再实现最小代码使其通过。
- 核心测试必须覆盖正常流程、非法状态、越权、跨网点访问、并发竞争、幂等重放和事务回滚。
- 集成测试使用 PostgreSQL，不用 SQLite 替代 PostgreSQL 特有行为。
- 每个阶段结束前必须运行 Ruff、mypy、pytest、迁移升级和阶段 HTTP 旅程测试。
- 声称阶段完成前必须提供最新验证命令及其实际输出，不以推测代替验证。
- 阶段内不得留下阻塞后续开发的占位实现、未说明的 TODO 或跳过测试。

## 9. Git 与变更控制

- 保持提交小而聚焦，一个提交只表达一个可验证的意图。
- 不覆盖或删除用户已有变更，不使用破坏性 Git 命令。
- 提交前检查差异、格式、类型和测试结果。
- 数据库模型变化必须同时提交对应 Alembic 迁移和测试。
- 实现范围以已确认的设计、PRD 和当前阶段计划为准；扩大范围前先取得用户确认。

## 10. 完成定义

一个后端阶段只有同时满足以下条件才算完成：

1. 功能符合已确认设计和当前阶段范围；
2. 数据库可以从空库执行全部迁移；
3. Ruff、mypy 和相关 pytest 测试通过；
4. 关键流程可以通过真实 HTTP 接口演示；
5. 权限、安全、幂等和失败路径已验证；
6. 中文关键注释与必要文档已同步；
7. 不存在会阻塞下一阶段的临时代码。
