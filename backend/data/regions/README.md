# 中国行政区划数据

`cn-regions-2022.json` 由用户提供的 `2022年行政区划代码.xlsx` 转换得到，作为
Yitu 地址选择与区县代码解析的固定版本数据源。

- 范围：中国大陆省、市、区县三级。
- 版本：2022。
- 运行方式：Alembic 迁移 `0032` 自动导入，部署时不依赖原始 Excel。
- 更新方式：从新的可信行政区划源生成新版 JSON，并新增迁移进行幂等升级；历史区域应停用而不是物理删除。
