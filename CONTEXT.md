# 项目上下文

## 当前方向

- 先完成六个后端阶段，再冻结 OpenAPI，最后开发前端。
- AI Agent 是核心模块，但阶段一只建设所有后续模块共用的可靠基础。
- RAG 文档由管理或运营人员通过前端接口上传。
- 客户既可以在 AI 对话中下单，也可以自行填写表单下单；两个入口共用同一业务服务。

## 阶段一状态

- 已完成：配置、统一时区、错误协议、PostgreSQL、迁移、幂等、审计、Outbox、重试、死信和 Celery Worker。
- 正在完成：Docker Compose、就绪检查和阶段总门禁。

## 开发约束

- 新增注释和 docstring 使用简体中文。
- 密钥只写入本地 `.env`，不得提交。
- 普通任务保留少量关键测试；数据库并发、权限、状态机、RAG 和 Agent 才进行专项验证。
- Git 提交信息使用中文。
- Celery 固定使用独立的 `yitu` 队列，避免消费同一 Redis 中其他项目的默认任务。
